namespace Day4
{
    internal class Matcher
    {
        private readonly string _pattern;
        private readonly Direction _direction;

        public Matcher(string pattern, Direction direction)
        {
            _pattern = pattern;
            _direction = direction;
        }


    }
}
